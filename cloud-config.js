// Cloudinary configuration — used for vendor document uploads (bills, contracts, screenshots).
// Free tier: 25GB storage, no credit card required. See README.md for setup steps.
//
// These two values are safe to be public — an unsigned upload preset only allows uploads,
// never deletion or access to your account, so there's nothing sensitive here.

export const CLOUDINARY_CLOUD_NAME = "ub7ffrzp";
export const CLOUDINARY_UPLOAD_PRESET = "wedding-planner";
